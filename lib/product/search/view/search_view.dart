import 'package:car_app/core/init/view/base/base_stateless.dart';
import 'package:flutter/material.dart';

class SearchView extends BaseStateless {
  SearchView({Key? key}) : super();

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: ListView(
        children: const [],
      ),
    );
  }
}
